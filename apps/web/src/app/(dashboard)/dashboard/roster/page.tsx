import type { Metadata } from "next";
import RosterPage from "../../(talent-manager)/roster/page";

export const metadata: Metadata = {
  title: "Dashboard Roster",
  robots: { index: false, follow: false },
};

export default RosterPage;
