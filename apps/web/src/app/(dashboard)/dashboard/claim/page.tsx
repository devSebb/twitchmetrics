import type { Metadata } from "next";
import ClaimProfilePage from "../../(creator)/claim/page";

export const metadata: Metadata = {
  title: "Dashboard Claim",
  robots: { index: false, follow: false },
};

export default ClaimProfilePage;
